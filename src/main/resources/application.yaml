spring:
  application:
    name: auth-server

  #  profiles:
  #    active: dev
  security:
    oauth2:
      client:
        registration:

          auth-server-service:
            provider: auth-server
            client-id: ${AUTH_SERVER_CLIENT_ID:dummy}
            client-secret: ${AUTH_SERVER_CLIENT_SECRET:dummy}
            authorization-grant-type: client_credentials

          google:
            client-id: ${GOOGLE_CLIENT_ID:dummy}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy}
            authorization-grant-type: authorization_code
            scope:
              - profile
              - email
              - openid

          github:
            client-id: ${GITHUB_CLIENT_ID:dummy}
            client-secret: ${GITHUB_CLIENT_SECRET:dummy}
            authorization-grant-type: authorization_code
            scope:
              - user
              - user:email

        provider:
          auth-server:
            token-uri: http://127.0.0.1:9000/oauth2/token

  mvc:
    apiversion:
      use:
        header: API-Version
      default: v1
      required: false
      detect-supported: true
      supported:
        - v1

  threads:
    virtual:
      enabled: true

  config:
    import: optional:file:.env[.properties]

  docker:
    compose:
      #      enabled: true
      file: local-infra/docker-compose.yaml

  jpa:
    open-in-view: false

  main:
    banner-mode: off

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  session:
    data:
      redis:
        namespace: auth:session
        repository-type: default

  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:dummy}
    password: ${MAIL_PASSWORD:dummy}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  base-url: ${APP_BASE_URL:http://localhost:9000}

gateway:
  client:
    secret: ${GATEWAY_CLIENT_SECRET:dummy}

aws:
  public_key: ${AWS_PUBLIC_KEY:dummy}
  private_key: ${AWS_PRIVATE_KEY:dummy}
  region: ${AWS_REGION:eu-north-1}
  arn: ${AWS_ARN:dummy}
  bucket_name: ${AWS_BUCKET_NAME:dummy}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: when-authorized

server:
  forward-headers-strategy: framework